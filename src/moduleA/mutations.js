export default {
    // addCount(state, num) {
    //     return state.count += num
    // }
    // addCount(state, num) {
    //     return state.count += payload.num
    // }
    addCount(state, num) {
        return state.count += num
    }
}